#ifndef __GRIDINFO_HPP
#define __GRIDINFO_HPP

#include <iostream>
#include <map>
#include <string>

#include "murphy.hpp"

using namespace std;

// class gridinfo_t {
//    protected:
//         std::map<std::string, sid_t> lda;

//         p8est_mesh_t* mesh;
//         p8est_ghost_t* ghost;
//         real_t*        mirrorblocks = nullptr;  // contains the max allocated memory for the mirrors
//     real_t*        ghostblocks  = nullptr;  // contains the max allocated memory for the ghosts values
        

//     public:
//     add_field(string name);
//     remove_field(string name);
// };

#endif